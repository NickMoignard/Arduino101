
//  set constants
  //  red diode pin                  
  //  green diode pin
  //  blue diode pin

// initialize variables
  //  colourUp will start with red
  //  colourDown will start with greed
  //  unused colour will start with blue
  //  set the delay length to be something small like 5 - 50 milliseconds
  //  brightness counter variable
  //  temporary storage variable for the shuffling of colours
  

void setup() {
  
  //  set pinModes 

  //  Open Serial communications
  //  print somehting to the console
}

//  create a reset and swap function.  
void resetAndSwap() {
    //  reset counter at zero
    //  print to the monitor
    
//shuffle colours around in variables                                                      
    //  move the unused colour into temporary variable 
    //  store the colour at zero brightness in colour off variable
    //  store the colour at full brightness in colour down variable
    //  move the prevoiusly unused colour from temporary variable into colourUp variable        
}
  
void loop() {
  //  increment brightness counter

  if (/*insert conditional*/) { //  check if brightness counter is greater than 255, if so reset and swap colours
      //  execute the resetAndSwap function
  }
  //  call analogWrite *note colour1 is going up in brightness
  //  call analofWrite *note color2 will be going down in brightness
  //  lets include a delay
}

